#How to use
This is built as an extension, so you need chrome based browser. First, go to chrome://extensions, then in the top right, enable develper mode, then go into realeses on this repo to accsess it. Extract the zip, then add that using the load unpacked.
